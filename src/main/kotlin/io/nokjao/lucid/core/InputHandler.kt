package io.nokjao.lucid.core

import io.nokjao.lucid.core.interfaces.*

class InputHandler : Handler {
    override fun handle(): ICommand {
        TODO()
    }
    fun handleInput(): ICommand {
        return handle()
    }
}